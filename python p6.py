data=[10,20,30,40,50,60,70]
def remove.duplicates(duplist):
    no duplist=[]
    for element in duplist:
        if element not in duplist:
            noduplist.append(element)
            return noduplist
        print(remove.duplicates(data))
        
